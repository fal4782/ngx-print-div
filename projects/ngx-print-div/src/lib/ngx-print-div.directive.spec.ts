import { NgxPrintDivDirective } from './ngx-print-div.directive';

describe('NgxPrintDivDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxPrintDivDirective();
    expect(directive).toBeTruthy();
  });
});
